# BasicCurrentFlow

main: kit: flow: BasicCurrentFlow:
{
  entity: "current",
  kind: "basic",
  name: "BasicCurrentFlow",
  step: [
    {
      input: {
        ref: "current_ref01",
        srcdatavar: "current_ref01_data",
        suffix: "_dt0"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-current_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicCurrentFlow"
}